# pnpm is the package manager for this repo (see packageManager in package.json).
# pnpm 10+ does NOT read project settings from this file — its guards live in
# pnpm-workspace.yaml. The keys below only take effect if someone runs npm.
ignore-scripts=true
min-release-age=7
allow-git=none
save-exact=true