# Connection string used by the drizzle client (lib/db/client.ts)
# and by the drizzle-kit commands (db:migrate, db:push, db:studio).
DATABASE_URL=mysql://root:secret@localhost:3306/solara

# Signs the session cookie. Generate one with: openssl rand -base64 32
SESSION_SECRET=change-me
