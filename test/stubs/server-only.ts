/**
 * Stub for the `server-only` package.
 *
 * The real module throws unless it is resolved through the `react-server`
 * export condition, which Jest does not set, so importing any server module
 * under test would fail.
 */
export {}
